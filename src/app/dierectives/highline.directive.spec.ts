import { HighlineDirective } from './highline.directive';

describe('HighlineDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlineDirective();
    expect(directive).toBeTruthy();
  });
});
